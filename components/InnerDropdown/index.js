import InnerDropdown from "./InnerDropdown.vue";

const _InnerDropdown = {
  install(Vue) {
    Vue.component(InnerDropdown.name, InnerDropdown);
  }
};

export default _InnerDropdown;

export { InnerDropdown };